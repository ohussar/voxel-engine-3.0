#version 400 core
in vec3 pass_coords;
out vec4 outColor;
uniform sampler2DArray textureSampler;
void main()
{
    outColor = texture(textureSampler, vec3(pass_coords.x, -pass_coords.y , pass_coords.z));
}